
{
  "rules": {
    "databases": {
      "{database}": {
        "documents": {
          "artists/{artistId}": {
            "allow read": "if true;",
            "allow write": "if request.auth != null;"
          },
          "config/{docId}": {
            "allow read": "if true;",
            "allow write": "if request.auth != null;"
          },
          "{document=**}": {
            "allow read, write": "if request.auth != null;"
          }
        }
      }
    }
  }
}
